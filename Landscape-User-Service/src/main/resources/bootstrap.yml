spring:
  application:
    name: Landscape-User-Service
  
  cloud:
    config:
      uri: http://localhost:8888
      

#logging:
#  level:
#    root: debug
logging:
  file: logs/Landscape-User-Server.log    
  pattern:
    file: '%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] %-5level %logger{36}.%M - %msg%n'
  level:
    root: debug