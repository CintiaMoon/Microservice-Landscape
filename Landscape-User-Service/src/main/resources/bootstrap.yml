spring:
  application:
    name: Landscape-Private-Service
  
  cloud:
    config:
      uri: http://localhost:8888
      

#logging:
#  level:
#    root: debug
logging:
  file: logs/Landscape-Private-Server.log    
  pattern:
    file: '%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] %-5level %logger{36}.%M - %msg%n'
  level:
    root: debug